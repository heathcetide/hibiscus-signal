package com.hibiscus.signal;

public class SignalsTest {
    public static void main(String[] args) {
//        Signals sig = Signals.sig();
//        long connect = sig.connect("event001", (sender, params) -> {
//            System.out.println("Sender: " + sender + " Params: " + Arrays.toString(params));
//        });
//        sig.addFilter("event001", (event, sender, params) -> {
//            // 例如，拦截特定条件下的事件
//            if (params != null && params.length > 0 && params[0].equals("block")) {
//                return false;  // 阻止信号传播
//            }
//            return true;  // 允许信号传播
//        });
//        sig.emit("event001", "参数", (error) -> {
//            System.out.println("Error: " + error.getMessage());
//        },"allow");
////        sig.disconnect("event001",connect);
//        sig.emit("event001", "参数", (error) -> {
//            System.out.println("Error: " + error.getMessage());
//        },"block");
    }
}
